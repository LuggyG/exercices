function range(ascend, descend) {
  // Code the function here.
let newArray = [];
if (ascend > descend){
  while (ascend = descend){
    ascend ++;
    console.log(range)
  }
}
};

const ascArray = range(1, 4);
// => [1, 2, 3, 4]

const descArray = range(4, 1);
// => [4, 3, 2, 1]
// Do not remove last lines, it is for tests
module.exports = range;

// PAS FINIS 